{
  "name": "21mlabs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "21mlabs.com — static site, hand-rolled with 11ty.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve --quiet",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5"
  }
}
